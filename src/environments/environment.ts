export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  wsUrl: 'ws://localhost:8080/ws',
  appName: 'Travner',
  version: '1.0.0',
  // OpenRouter configuration (handled by backend)
  openRouter: {
    enabled: true,
    // Note: API key is handled by backend for security
  }
};

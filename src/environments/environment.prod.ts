export const environment = {
  production: true,
  apiUrl: 'https://travner-web-backend-production.up.railway.app/api',
  wsUrl: 'https://travner-web-backend-production.up.railway.app/ws',
  appName: 'Travner',
  version: '1.0.0',
  // OpenRouter configuration (handled by backend)
  openRouter: {
    enabled: true,
    // Note: API key is handled by backend for security
  }
};
